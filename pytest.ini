# Keep collection out of scripts/ -- scripts/smoke_test.py matches the
# *_test.py pattern and parses CLI args at import time, which breaks bare
# `pytest -q` invocations from the repo root.
[pytest]
testpaths = tests
