# Agentic HSE integration notes

This draft branch adds a minimal `agentic_hse/` package so the worker can grow into a LangGraph-orchestrated HSE assistant without replacing the current vision routes.
